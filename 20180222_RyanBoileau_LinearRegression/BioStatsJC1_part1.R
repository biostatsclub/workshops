#Assigning vectors to a variable using the combine function c()
a <- c(1,2,5.3,6,-2,4) #numeric vector
b <- c("one","two","three") #character vector
c <- c(TRUE,TRUE,TRUE,FALSE,TRUE,FALSE) #logical vector

# Values of height
#151, 174, 138, 186, 128, 136, 179, 163, 152, 131
# Values of weight.
#63, 81, 56, 91, 47, 57, 76, 72, 62, 48

#Using data above, generate vectors for these sets of numbers

#Call the variables in the console

#The built-in function lm() makes for easy linear modeling
#lm(y~x, options...)


#See the attributes of your regression analysis


#Let's plot the regression and add a best fit line using abline(REGRESSIONVARIABLE) inside plot()







